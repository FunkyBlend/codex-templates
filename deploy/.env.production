# Production environment for Codex Marketplace
# No database needed — app uses static mock data
# Add DATABASE_URL here if you connect a real PostgreSQL instance later

NODE_ENV=production
PORT=3001
